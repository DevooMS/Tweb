
  /*Blueside Esteso*/
  .sidebar{
    width:6.5rem;
    min-height:100vh
  }
  
  .sidebar .nav-item .nav-link{
    text-align:center;
    padding:.75rem 1rem;
    width:6.5rem;
    font-size:.65rem
  }
  /*Logo e poi test*/
  .sidebar .nav-item .nav-link span{
    display:block
  }
  /*Font size*/
  .sidebar .nav-item .nav-link i{
    font-size:1rem
  }
  /*Spessore Font*/
  .sidebar .nav-item .nav-link.active{
    font-weight:700
  }
  .sidebar .nav-item .collapse{
    position:absolute;
    left:calc(6.5rem + 1.5rem / 2);
    z-index:1;
    top:2px
  }
  .sidebar #sidebarToggle{
    width:2.5rem;
    height:2.5rem;
    text-align:center;
    margin-bottom:1rem;
    cursor:pointer
  }
  .sidebar #sidebarToggle::after{
    font-weight:900;
    content:"";
    font-family:"Font Awesome 5 Free";
    margin-right:.1rem
  }
  .sidebar #sidebarToggle:hover{
    text-decoration:none
  }

 

  @media(min-width: 768px){
    .sidebar{
        width:14rem !important
    }
    .sidebar .sidebar-brand .sidebar-brand-text{
        display:inline
    }
    .sidebar .sidebar-heading{
        text-align:left
    }
   /*Sidebar Toggle serve per nascondere la bara ma non in modo permanente*/
    .sidebar.toggled{
        overflow:visible;
        width:6.5rem !important
    }
    .sidebar.toggled .sidebar-brand .sidebar-brand-text{
        display:none
    }
  }
  
  
  
  /*colore trasparente attorno la freccia*/
  .sidebar-dark .nav-item .nav-link[data-bs-toggle=collapse]::after{
    color:rgba(255,255,255,.5)
  }
  
  .sidebar-dark #sidebarToggle{
    background-color:rgba(255,255,255,.2)
  }
  /*Colore Freccia*/
  .sidebar-dark #sidebarToggle::after{
    color:rgba(255,255,255,.5)
  }
 
  
  /*Colore Soffuso sidebar*/
  .bg-gradient-primary{
    background-color:#4e73df;
    background-image:linear-gradient(180deg, #4e73df 10%, #224abe 100%);
    background-size:cover
  }

  
  
  /*Centro Immagine del login e dell password */
  .bg-login-image {     
    background-position:center;
    background-size:auto
  }
  .bg-register-image{
    background-position:left;
    background-size:auto
  }
  /*Contenimento elemento sidebar e il resto della pagina*/
  #wrapper{
    display:flex
  }
  #wrapper #content-wrapper{
    background-color:#f8f9fc;
    width:100%;
    overflow-x:hidden
  }
  #wrapper #content-wrapper #content{
    flex:1 0 auto
  }
  /*imposto il colore background*/
  .bg-gradient-primary{
    background-color:#0d49fd;
    background-image:linear-gradient(180deg, #d3f800 100%);
    background-size:cover
  }
  